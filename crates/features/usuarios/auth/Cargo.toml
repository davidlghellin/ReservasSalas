[package]
name = "usuarios-auth"
version = "0.1.0"
edition = "2021"

[dependencies]
usuarios-domain = { path = "../domain" }

# JWT
jsonwebtoken = "9.3"
serde = { workspace = true, features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }

# Password hashing
argon2 = "0.5"
